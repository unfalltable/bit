# BIT 规格差异与待决策清单

日期：2026-09-08。状态：开发前审阅记录；所有待修复和待决策事项尚未因本文自动完成或获批。

主线：[项目梳理与总体开发方案](D:/others/BIT/docs/BIT_项目梳理与总体开发方案.md)。

后续更新：第一轮桌面证明和基础共识实验已经运行，Android 按用户要求跳过；详见 [技术验证报告](D:/others/BIT/docs/BIT_第一轮技术验证报告.md)。第 6 节保留此前梳理阶段的核验记录，待修复合同与完整验收未因此自动完成。

## 1. 资料层级和实际状态

| 资料 | 当前内容 | 本次如何使用 |
|---|---|---|
| [BIT 单文件 v1.2](D:/others/BIT/docs/BIT_完整开发规格_Codex版_v1.2_1024亿固定总量.md) | BIT 命名、1024 亿累计上限、有限预算减半、20 章及部分合同附录 | 作为最新主规格；其中主网未批准项继续保持待定 |
| [Solo README](D:/others/BIT/docs/solo/README.md) 与 20 个分章 | 旧 Solo/SLC/uslc，持续年率发行；大量非经济需求与新版一致 | 补充上下文，并识别旧规则；不让旧经济条款覆盖新版 |
| [旧配置](D:/others/BIT/docs/solo/config/params.reference.json) | spec_version=1.0、solo-test-1、年率 200 bps、技术上限 10^26 原子单位 | 用于比对；不能直接作为 BIT 配置 |
| [旧合同](D:/others/BIT/docs/solo/contracts/openapi.yaml) | OpenAPI 3.0.3、旧 Supply/Network、41 个本地 SDK 方法 | 保留结构，后续按新版补齐 |
| [任务](D:/others/BIT/docs/solo/codex/tasks.json) 与 [状态](D:/others/BIT/docs/solo/codex/implementation_status.json) | 30 项、旧命名路径、全部 NOT_STARTED | 用作拆分起点；本次不标实现或通过 |
| [旧参考模型](D:/others/BIT/docs/solo/reference/accounting_model.py) | 单池整数会计、旧年率和按当前存量裁剪发行 | 可参考份额/退出数学，不能作为 BIT 新发行 oracle |
| [旧验证报告](D:/others/BIT/docs/solo/VALIDATION_REPORT.md) | 文档结构与 20 项参考模型测试历史报告 | 仅限其对象和范围 |

工作目录当前仅包含 `docs`。未发现产品 Rust/Flutter/React 工程、项目构建脚本或已运行链的证据。Solo 包的 `AGENTS.md` 和主规格附录中的开发启动指令属于本次阅读资料，不扩展用户“先梳理”的请求范围。

20 个 Solo 分章已与 BIT 主规格逐行对照，比较时归一化 Solo/SLC/uslc 和 bit/solo 前缀。主要实质变化集中在发行、相关状态/接口/参数以及命名；相同业务、安全、交互内容可继承，不能因此假定整个旧包兼容新版。

## 2. 已确认的版本差异

P0 表示进入依赖该内容的开发前应解决；P1 表示应在对应模块冻结前解决。它们是实施优先级，不是对已上线软件的漏洞评级。

| ID | 优先级 | 已确认问题及依据 | 处理建议与验收 |
|---|---|---|---|
| GAP-01 | P0 | 最新单文件是 BIT v1.2，分章/合同/任务仍使用 Solo、SLC、uslc、solo.v1、SoloError 和旧签名域 | 建立活跃 BIT 基线；统一 URI、HRP、派生/签名域、类型和路径；原 Solo 包保留历史身份，不全目录盲替换 |
| GAP-02 | P0 | 旧第 06 章和 params 使用 2% 目标年发行及技术上限；与新版有限累计发行不兼容 | 按新版第 06 章重建参数、计数器、oracle、状态和测试；活跃链配置不得残留旧年率入口 |
| GAP-03 | P0 | 旧 accounting_model 的发行空间基于 cap-current_supply；销毁后可重新产生发行空间 | 新模型以 I=G0+Mint 审核累计上限，并匹配精确 epoch 配额；增加销毁后不补发、空集合永久未发和重放负例 |
| GAP-04 | P0 | 新文档引用完整 v1.2 ZIP 包，但当前目录未包含对应完整拆分文件 | 从单文件附录恢复已有合同，并补齐未附内容；记录来源和人工补写边界，不声称只是解压了缺失 ZIP |
| GAP-05 | P0 | 新版要求 OpenAPI 3.1.0；旧文件为 3.0.3，Network/Supply 缺新政策及发行计数器 | public.schema 与 OpenAPI 同步更新；验证 JSON Schema 方言、引用及真实代码生成 |
| GAP-06 | P0 | 新版 SDK 应增加 network_supply_verified，总计 42；现有机器合同只有 41 且错误类型为 SoloError | 补齐只读供给 DTO、验证语义、错误和 FFI 字符串金额；旧客户端缺字段须识别不兼容 |
| GAP-07 | P0 | 新版要求 178 项计划验收；当前矩阵为 150，缺 28 项 T-MON | 补完整 T-MON，并映射需求、任务、代码与命令；初始状态仍为未执行 |
| GAP-08 | P1 | 新 SQL 附录有累计上限和计数恒等式，旧独立 SQL 无对应完整货币字段 | 将新快照映射到 NUMERIC，执行实际迁移和大于 2^63 的序列化检查；同时验证链状态与证明 |
| GAP-09 | P0 | v1.2 附录记载“110 项结构检查、64 项参考测试”，本地脚本和结果属于旧包 | 新旧报告分开；获得完整新版代码后重新执行，不能复制附录数字覆盖现有证据 |
| GAP-10 | P1 | 原任务顺序把 signer、统一隐私网络、CLI 和 localnet 工具放在其被需要之后 | 拆前置核心与后续完整交付；重建任务 DAG，确保测试可以从核心开发开始执行 |
| GAP-11 | P1 | 旧 mainnet-inputs.schema 只要求 mainnet_ready=false；描述引用 D-028，任务 D-028 实为隐私负例 | 编写正式 manifest/evidence schema 和真实校验；明确由创世/发布任务负责，布尔值不能代表完整预检 |
| GAP-12 | P1 | 新版出现重复的 10.5、16.6 小节号；存在单文件与分章的维护漂移 | 统一分章源、索引和生成过程；章节编号与跨文件引用检查纳入规格维护 |

### 2.1 新版独立文件缺失清单

下列文件在当前 `docs/solo` 包中不存在；其中一部分内容已嵌入 BIT 单文件：

| 文件 | 单文件中是否能取得相关内容 | 后续动作 |
|---|---|---|
| `CHANGELOG_BIT命名.md` | 有完整附录 | 拆出并保留来源 |
| `CHANGELOG_1024亿固定总量.md` | 有完整附录 | 拆出并保留来源 |
| `contracts/bit.proto` | 有完整附录 | 拆出、生成并验证；旧 solo.proto 不继续作为活跃合同 |
| `contracts/monetary_policy.cddl` | 有完整附录 | 拆出，并用独立编码实现交叉验证 |
| `tests/emission-vectors.json` | 有固定向量附录 | 拆出、核验、纳入新 oracle 与执行器测试 |
| `reference/halving_model.py` | 只有公式和说明，没有完整程序 | 需要实现或取得新版源文件 |
| 新版参考模型扩展及桥接测试 | 只有用途、结果数量和部分向量 | 需要实现或取得，不能据此宣称 64 项已通过 |
| `tools/build_spec.py` | 只有历史执行说明 | 需要建立可复现生成流程 |
| 新版 OpenAPI/public.schema/local_sdk/acceptance-matrix | 有增量说明，未附全量机器文件 | 基于旧结构逐项升级，并执行完整合同校验 |

新版参数、主网输入模板、动作会计和 SQL 也在附录中给出了内容；可以用于后续同步，但不能只同步这些可见片段后就宣布整个新版包完整。

### 2.2 建议建立唯一开发来源

后续建议建立 `docs/spec` 分章、`contracts`、`config`、`reference`、`tests/vectors` 和任务目录作为活跃开发基线，再生成单文件阅读版。`docs/solo` 保持只读历史参考，不作为自动代码生成和默认启动器的输入。

配置只保留一个参考数值源；OpenAPI/Schema/SDK 共享金额与公共 DTO 定义，明确哪个文件是源、哪个是生成物。文档版本 1.2、交易 wire version 1、接口路径 /v1 是不同维度，不机械改成同一个数字。

这是一项下一阶段建议，本次没有搬动原文件、改写合同或初始化产品工程。

## 3. 还需补齐的协议合同

以下属于“规格尚需精确定义或验证”，不代表已经发现生产漏洞。建议落成小范围 ADR、字节合同和固定向量后再实现依赖模块。

| ID | 待定内容 | 为什么需要 | 建议产物与责任阶段 |
|---|---|---|---|
| SPEC-01 | genesis manifest 的规范编码、hash 输入、签名覆盖范围和生成次序 | chain_context 来自 genesis_manifest_hash，而初始 position/state 又使用 chain_context；如把派生后的状态根无条件放回被哈希主体，可能形成循环依赖 | 画清哈希依赖图，区分身份输入与派生输出；S0/S1 固定，D-025 实现 |
| SPEC-02 | native_asset_id、地址 HRP、恢复词到 root、支付/operator 等完整派生规则 | 文档列出若干域及 manifest 必填项，尚缺完整可交叉复现的 manifest/向量 | 上游兼容说明、完整 key/address vectors、所有平台一致性 |
| SPEC-03 | compact canonical_records、树增量、memo、恢复收据正文、公开事件的精确编码 | 执行摘要、紧凑交易、TCT 结果根和当前系统公共事件已冻结 v1 字节合同与黄金向量；完整归档、恢复收据正文和客户端分发仍未完成 | 已落地 [D-002 区块产物规范编码](D:/others/BIT/docs/BIT_D-002_区块产物规范编码.md)；D-003/010/012 继续完成服务与客户端边界 |
| SPEC-04 | supply/audit_snapshot 的规范值编码与 REST 映射 | v1 共识值、JMT 原子写入、最新/精确历史高度 ICS23 证明、共享向量和字段篡改负例已完成；REST DTO 和 SDK 解码仍待接入 | 已落地 [D-006 固定总量与支付会计](D:/others/BIT/docs/BIT_D-006_固定总量与支付会计.md)；D-022 继续完成服务与客户端边界 |
| SPEC-05 | 检查点的到期字段/推导规则、发布者名单、签名阈值、轮换与撤销语义 | 正文要求生成时间和到期时间，旧 Checkpoint 必填列表只有 issued_at，未明确到期表达 | 规范消息与信任状态机；测试过期、冲突、错签名者和长期离线 |
| SPEC-06 | 节点请求、配对挑战、最终 PoP、动画 QR 分片与已认证本地传输 | 现有描述很明确，但缺完整的机器消息和多轮交互合同 | typed messages、大小/过期/重放限制；D-012/019/020 联合验证 |
| SPEC-07 | “选择多个退出领取”的实际协议含义 | 每笔最多一个业务动作，ClaimExit 只含一个 ticket_id | 建议 UI 批量选择后逐笔生成/确认明确授权的交易包，列总费用和部分成功状态；如要单交易多票据必须另改协议 |
| SPEC-08 | 创世“可选公开锁定规则”的具体支持集合 | 当前说不支持自定义释放脚本，但未给完整有限锁定类型与执行语义 | 先定义枚举和编码；主网分配不能包含尚未实现的释放安排 |
| SPEC-09 | 选择性付款凭证的字段、可验证能力与泄露范围 | W17 提到导出凭证，但不能默认导出完整查看密钥或全部流水 | 钱包/SDK 的本地导出合同和对外核验说明；明确对方能验证什么 |
| SPEC-10 | 正式验收负载与资源参数的对应关系 | 20 TPS、1 年恢复、24 小时补扫及手机证明目标没有完整共同负载定义 | 固定设备、交易形状、compact 大小、网络条件、样本量和失败阈值 |

SPEC-01 的关键是先明确哈希主体。本文未替协议决定新的 chain_context 公式，也未把循环依赖的可能性当作已确认故障。

## 4. 待用户或真实参与者决定的事项

无需重新确认 BIT 命名和 1024 亿累计上限。下表登记的是原文保留的未知，以及实际实施需要的约束；本次没有自动采用默认项作为主网政策。

| ID | 决策 | 当前状态/建议准备方式 | 何时必须落定 |
|---|---|---|---|
| DEC-01 | 主网创世发行量 G0 | 未定；测试 1 亿仅作夹具 | 主网经济配置冻结前 |
| DEC-02 | 分配名单、各份额、初始自质押及锁定 | 无真实名单和授权；不默认给团队预留比例 | 创世构建与签署前 |
| DEC-03 | 减半算法、H/L、永久未发政策 | 采用第 06 章作为研发参考，约四年周期及政策仍待评估批准 | 主网货币政策哈希冻结前 |
| DEC-04 | 委托门槛、自质押、活跃席位、佣金及处罚参数 | 1 BIT/1000 BIT/64 席位等为参考；需要经济与运行评估 | 正式 genesis 参数冻结前 |
| DEC-05 | 手续费和长期网络服务预算 | 需评估低交易量下验证者、网关、归档和检查点的持续运营 | 主网经济评估与运维准备阶段 |
| DEC-06 | 首批测试设备、构建环境与可投入资源 | 建议 Linux 链集成、Android/桌面尽早真机验证，并提前检查 Apple 平台构建 | S1 开始前明确可用资源 |
| DEC-07 | 用户流量、同步和恢复体验预算 | 需选定日常负载与能接受的数据量/时间，再反推容量 | 性能 profile 和主网容量冻结前 |
| DEC-08 | 真实独立运营者与服务来源 | 包括验证者、哨兵、查询、归档、检查点发布者；多域名不自动等于独立 | 公共测试与主网准备阶段 |
| DEC-09 | 独立审查、复现构建与签名发布主体 | 需要真实人员、版本范围、结果与签名材料 | 正式发布前 |
| DEC-10 | 发行/服务地区、分发渠道与名称标识 | 资料未确认；按实际用途取得相应评估和分发材料 | 对外商业化和正式分发前 |

主网外部材料未齐不会阻止本地网、测试网、完整软件和预检工具开发。预检必须精确报告缺项，不以 `economic_policy_approved=true` 或 `mainnet_ready=true` 替代签署内容和证据验证。

## 5. 建议优先创建的开发前产物

这些是下一阶段的可审阅交付项，本次尚未生成对应生产合同：

1. `SPEC_BASELINE.md`：来源优先级、版本、迁移范围和历史资料使用规则。
2. 同步后的 BIT 分章、参数与机器合同：包含命名、新发行、金额类型和所有新接口字段。
3. 货币模型包：减半 oracle、池会计适配、桥接测试、固定向量和 28 项 T-MON 计划。
4. 协议补充合同：完成 SPEC-01～SPEC-08 中会阻塞类型、同步和密钥工作的内容。
5. 更新任务 DAG：明确 signer/Arti/CLI/localnet 前置部分，完整 30 项范围继续可追踪。
6. 技术验证任务卡：每项写输入版本、实验步骤、实际产物、成功/失败标准及后续影响。
7. 验收覆盖矩阵：PR 需求 → 业务动作/页面 → 模块 → 测试 → 真实报告。

完成门槛应是“资料一致且关键输入可复现”，不以文件数量或占位脚本存在作为通过标准。

## 6. 本次实际核验记录及其限制

### 6.1 本次执行

| 检查 | 实际结果 | 结论范围 |
|---|---|---|
| 文件和任务清点 | 20 个旧分章、65 页、19 个旧 REST 操作、41 个 SDK 方法、30 项任务、150 项旧计划验收 | 现有规格资产的数量，不是产品实现数 |
| 任务状态解析 | 30 项全部 NOT_STARTED | 旧包未声明任何实际产品任务完成 |
| SHA256SUMS 对照 | 55 个条目逐一匹配，无缺失或哈希不符 | 现有 Solo 包与其自带摘要一致；摘要不证明来源签名或新版兼容性 |
| 旧整数模型复跑 | 20 项通过 | 单池会计 oracle，不含真实证明、共识、数据库或钱包 |
| BIT 内嵌发行向量复算 | 11 个 reference_points 的累计配额与下期配额均匹配公式 | 指定点的整数算法核对，不是完整新模型测试套件 |
| 内嵌政策哈希复核 | 附录 CBOR 十六进制字节的 SHA-256 与给定值一致 | 未使用独立 CBOR 库重新构建语义对象，不代表规范编码已交叉验证 |
| 上游代码/规范抽查 | Penumbra v2.1.1 构建版本、CometBFT 0.38.23 集合延迟及证据年龄规则得到核对 | 仅核对这些事实；未完成依赖冻结、漏洞适用性或整栈兼容审查 |

旧模型复跑工作目录为 `D:/others/BIT/docs/solo`，命令：

```powershell
py -B -m unittest discover -s reference -p 'test_*.py' -v
```

Python 3.14 执行，实际返回 `Ran 20 tests ... OK`。`-B` 避免写入字节码缓存。本次没有重跑会覆盖旧报告的 `tests/validate_bundle.py`，没有修改旧验收 JSON、摘要或测试输出文件。

发行检查在只读脚本中从主规格提取 `reference_policy` 和 `reference_points`，按第 06 章重算 U(e) 及差分，并核对给定政策字节的 SHA-256。未将这次点检查记为 64 项新模型测试。

### 6.2 本次未执行

Rust/Flutter/CometBFT 构建；真实 ZK 证明生成与验证；真实多节点网络；UI 操作；OpenAPI/Protobuf/CDDL 全量生成验证；SQL 迁移；网络隐私抓包；设备性能；安装签名；外部安全审查；真实创世和主网发布。

因此，本文完成的是项目梳理、规格差异分析和开发方案。当前最有价值的下一步是 S0 统一基线，再推进 S1 的关键技术验证。
