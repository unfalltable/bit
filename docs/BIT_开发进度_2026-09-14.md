# BIT 开发进度（2026-09-14）

状态：D-002 区块产物编码与归档、D-004 状态恢复、D-005 ABCI 生命周期、D-006 供应审计与 D-022 只读网关继续推进；S0、D-001 至 D-008 均为 `IN_PROGRESS`，D-022 已进入首批实现，其余后续任务尚未进入完整实现。按 D-001 至 D-030、产品、运维、独立审计和主网外部输入计算，完整上线约完成 26%–30%，链核心约完成 74%。主网未就绪，Android 按用户要求跳过。

## 本轮完成

UpdateValidator、UnjailValidator 和 RotateConsensusKey 已完成从规范 CBOR、结构校验、费用分类、角色授权、当前状态解析、状态转换、供应会计、JMT 持久化到 ABCI 调度的完整执行链。空 Update、落后 sequence、错误 operator、新共识键缺少 PoP 或重复使用历史/待生效密钥都会在写状态前拒绝。

验证人记录升级为 v3。公开资料立即更新；佣金降低排到下个 epoch，佣金上调每次最多 100 bps，并同时执行 604800 链秒与 120960 块通知期。停用在集合选择生效，重新启用回到 Candidate。Unjail 同时执行 7200 链秒与 1440 块等待。共识密钥轮换排到下个 epoch，生效时才替换当前密钥，并为旧密钥记录证据责任结束高度和时间；当前、历史、待生效密钥执行全局唯一约束。

状态先升级到 v8 并新增 `meta/block_time_seconds`，随后因逐验证人累计佣金升级到 v9。PrepareProposal、ProcessProposal 和 FinalizeBlock 从 ABCI v0.38 Timestamp 取 Unix 秒，状态提交与区块时间同批落盘并拒绝时间倒退。佣金、jail 和密钥历史因此只依赖共识链时间，不读取节点本地墙钟。

状态进一步升级到 v10：staking parameters v3 固定 10000 次机会与 9500 bps 阈值，validator v5 以压缩 bit 窗口持久化实际签名机会、签名计数、epoch 编号和累计 score。只有 Commit flag 按请求中的实际 power 加分；Nil/Absent 只形成未签机会，完整窗口低于阈值时 jail 并输出 power=0。

真实 Groth16 集成用例扩展为 RegisterValidator、Delegate、UpdateValidator、UnjailValidator、RotateConsensusKey 和 ClaimCommission 六类 enabled staking envelope；每类都执行 Spend/Output 证明、授权、binding、费用、状态落盘、供应守恒和重启恢复。直接状态测试另覆盖同块连续 sequence、失败原子性、待生效变更与时间倒退。

epoch 奖励已按 `score_v / total_score` 用 U256 中间乘法分配当时全部 F，再按验证人已生效佣金率拆入旧池 P 与 operator 累计佣金 C。两层取整余数保留在协议容器，池的 `last_settled_epoch` 单调推进。结算、发行/放弃、P/C/F 变更和 staking 记录在同一候选事务验证；ClaimCommission 通过公开 release 把 C 转回 Q，并把 fee 计入 F。

ABCI PrepareProposal、ProcessProposal 和 FinalizeBlock 现在分别严格解析 local/proposed/decided last commit，并调用同一系统阶段。新 epoch 首块先计入上一块机会，再自动执行 score 消费、发行/奖励、pending 激活、计划变更和集合选择；FinalizeBlock 把 key/power 差异编码为 Ed25519 ValidatorUpdates，移除或轮换旧键时返回 power=0。

状态升级到 schema v11，新增版本化 `staking/effective_schedule`。已提交高度 H 保存 H/H+1/H+2 的精确实际验证者集合；集合按 CometBFT 的 power/地址规则规范排序并用固定 Tendermint 实现计算哈希。PrepareProposal、ProcessProposal 和 FinalizeBlock 现在要求 32 字节 `next_validators_hash` 精确匹配 H+1，H>1 的 last commit 则逐项匹配 H-1 的地址、power 和顺序。H 返回的 ValidatorUpdates 只更新 H+2，Commit 和重启都会交叉验证质押逻辑集合与 H+2 集合。

最后有效验证者保护已经接入。如果 downtime jail 或集合选择会把非空实际集合变为空，共识系统阶段返回 `HALT_NO_SAFE_VALIDATOR_SET`。系统阶段任一错误都会回滚供应、质押、触及索引和集合日程的内存变更，Prepare 还有第二层集合一致性检查。

新增真实应用四节点集成探针。四个由 CometBFT Go module v0.38.23 构建的进程分别连接四个 `bit-app`，每个应用使用独立 JMT/RocksDB；实测 H=6 的奖励后 ValidatorUpdates 到 H=8 才生效，应用从高度 10 的 durable 状态重启后追块，四节点固定高度 app hash 一致，ABCI Query 返回 ICS23 proof。停止一半投票权后链停止，恢复第三个节点后继续出块。该探针使用五块 epoch 和临时域分离摘要，边界在报告中明确记录。

状态升级到 schema v12，新增逐验证人的 `staking/candidates/<validator_id>` 排序记录。池奖励、pending 激活、停用/恢复和 jail/unjail 都在同一候选事务中删除旧排名并写入新排名；集合选择直接遍历按 stake 降序、validator_id 升序的索引前 64 项。创世、区块提交和重启恢复都严格核对索引，持久化 pool 被篡改但索引未同步时会拒绝打开。

D-008 第一阶段接通 Unbond 和 ClaimExit。Unbond 按当前池汇率销毁部分或全部活动份额，以 position 执行前 sequence 派生 ticket，并按 validator/exit epoch 聚合 cohort；普通委托可全部退出，最后一个合格验证者的自质押不能主动清空。SHIELDED 与 RELEASED_VALUE 两种费用来源分别执行受限公开 release 和 `P/Q/X/F` 原子容器变换。

退出责任截止固定为当前 epoch 末高度加 2，达到截止块才记录共识链时间。只有高度和链时间都严格超过各自 unbonding 边界，cohort 才进入 Mature；ClaimExit 重新核对 ticket owner、sequence 和执行时精确 quote，最后一份领取 cohort 全部尾差，成功后永久标记 claimed。未成熟、重复领取、落后 sequence、低于 min gross 或高于 max fee 都整笔回滚。

状态升级到 schema v13，staking parameters v4 固定退出窗口、默认 5% Byzantine 罚没率和每块 128 个 cohort 上限；新增 `staking/exits/cohorts/<id>` 与 `staking/exits/tickets/<id>` 的版本化记录、触及写入、重启恢复、ICS23 证明及 `sum(X)=supply.exit_total` 交叉校验。跨七块测试覆盖退出、epoch 奖励交错、暴露截止、严格双成熟、领取和两次重启；真实 Unbond/ClaimExit envelope 另执行 Groth16、PositionOwner 签名、binding 与正式交易调度。

状态继续升级到 schema v14，新增按 deadline/cohort 规范排序的 exposure、maturity-height 和 maturity-time 三个持久化队列。系统只遍历当前已经到期的索引项；高度和链时间条件各自满足后对应项永久删除，两个成熟项都删除才置为 Mature。重启会交叉验证每个 cohort 的状态、时间与三个队列，缺项、孤儿项、错误 deadline 和陈旧项均拒绝；ICS23 测试覆盖 exposure 成员证明、转换后的非成员证明、双成熟成员证明和领取后的全部队列清理。

schema v15 已逐高度持久化真实验证者责任集合与共识链时间。记录使用严格版本化编码，并在最新高度与 H/H+1/H+2 日程交叉核对；历史高度支持精确查询、ICS23 证明和重启恢复，为后续 evidence 的成员、单个 power、总 power 与双年龄窗口校验提供可信输入。

状态现升级到 schema v16。ABCI `FinalizeBlock.misbehavior` 已严格归一化重复投票和轻客户端攻击证据；状态层按历史高度复核责任地址、验证人 power、集合总 power 和链时间，并按 CometBFT 规则只在块龄与时间龄都超限时判定过期。规范 evidence hash 绑定 chain context 和完整字段，重复提交不会重复处罚。

第一次有效重大证据会永久 tombstone 验证人、取消待生效变更、停止新委托并输出实际集合 power=0；当前活动池按 500 bps 从 P 扣除并累计 Burn。已有未成熟退出 cohort 按 `exposure_end_height >= infraction_height` 纳入同率扣罚，Pending 与独立佣金 C 不进入委托本金罚没。

`staking/slash_jobs/<validator_id>` 保存证据、冻结结束键、游标、处理数和 P/X 累计罚没，`staking/evidence/<hash>` 保存可证明的规范证据记录。所有任务共享每块最多 128 个 cohort 的全局上限；任务未完成时冻结相关 Unbond/ClaimExit，重启严格交叉核对任务、tombstone、cohort 索引和供应容器。集成测试用每块 1 项连续三块推进，并在每次提交后关闭、重启数据库，确认游标继续且不重复 Burn。

`HALT_NO_SAFE_VALIDATOR_SET` 已增加 JMT 外部的耐久停签记录。ABCI 在拒绝会清空实际集合的候选区块后，先将最后提交 app hash、危险区块上下文和完整规范 evidence 原子写入 `<state-dir>.safety-halt-v1`，再停止服务。记录带域分离校验和，启动会恢复完整临时文件并对损坏、冲突或 chain context 不符保持锁定。`safety_halt_admin` 可检查记录，并只在提供精确摘要时把它改名为审计归档；重放同一危险区块会再次产生相同停签记录。

处罚 prepare/commit 中断测试也已补齐：带 tombstone、P Burn、EvidenceRecord 和 SlashJob 的批次生成 app hash 后直接丢弃并关闭数据库，重启确认已提交高度、供应和验证人状态完全不变；相同证据重放得到相同 app hash，提交后只扣罚一次。

四节点网络现已注入真实重复投票证据。专用集成注入器读取隔离测试网的临时 FilePV 密钥，生成两个冲突且可由 CometBFT 历史集合验证的已签名 prevote，再通过标准 RPC 广播；注入器会拒绝不以 `bit-app-probe-` 开头的 chain ID。测试确认目标验证人在证据块 H+1 仍存在、H+2 被移除，四个独立 JMT 应用的规范证据记录、Burn 和固定高度 app hash 一致，证据记录查询带 ICS23 proof。处罚后只剩三个验证人时，停止其中一个有权节点会令剩余 power 恰为三分之二并停止出块，恢复该节点后四节点继续推进。

状态存储现使用仓库内固定 Cnidarium 0.83.0 最小补丁。原单次 RocksDB WriteBatch 改为启用 WAL 同步并传播写入/fsync 错误；写入前解析完整批次，只允许当前 Cnidarium 实际生成的列族 put/delete 编码，拒绝截断记录、错误计数、非法标签和越界 varint/value。补丁还提供统一版本历史快照重建。补丁保留 MIT 许可证，并用上游 crate 摘要及逐文件 SHA-256 清单约束未修改源码。

处罚测试加入两个 Commit 故障边界。损坏批次在触碰 WAL 前失败，已提交高度、Burn、EvidenceRecord 和验证人状态保持不变；同一证据重放仍产生同一 app hash。同步批次已经完整落盘、但进程在发布新内存快照前故障时，进程内视图保持旧高度并停止继续执行，关闭重启后从 RocksDB 恢复完整的新高度、tombstone、Burn 和相同 app hash。固定 Cnidarium 的 16 个单元测试、8 个启用的子存储集成测试和 3 个 WriteBatch 集成测试同时通过。

状态层现已增加可验证的本地快照导出与隔离恢复。导出通过固定 Cnidarium 的 RocksDB checkpoint 获取一致数据库，独立打开并执行全部状态不变量后，以规范 `manifest.bit` 绑定 schema、高度、链时间、存储版本、app hash、TCT 根、chain context、货币政策和全部文件的 4 MiB chunk SHA-256。导入拒绝额外文件、非规范或超限清单、链接/危险路径、错误配置、篡改 chunk、已有目标及源目录内目标；文件只写入隔离临时目录，完整打开并核对状态摘要后才原子发布。恢复测试确认 app hash、TCT、ICS23 证明不变，并能继续提交下一高度。

本地快照已接入 CometBFT ABCI v0.38 State Sync。format 1 使用 chunk 0 传规范 manifest，后续 4 MiB chunk 一一映射数据库内容，并限制总数不超过 100000。`OfferSnapshot` 只接受空应用，严格核对 snapshot 元数据、chain context、当前 schema，并把 manifest app hash 绑定到 CometBFT 轻客户端提供的可信 `app_hash`。接收端允许 chunk 乱序到达；manifest 到达后定位并删除坏块、要求重取并拒绝对应 peer，全部校验通过才在隔离目录重组并调用完整快照导入。

恢复后的数据库使用 snapshot ID 派生独立路径，带校验和的活动状态标记在同步落盘后原子发布，再替换当前应用状态。重启会按标记重新打开并核对高度、app hash 和 chain context；有效临时标记可从崩溃窗口恢复，冲突或损坏标记保持关闭。该目录仅包含应用状态，不触碰 CometBFT signer 防双签水位。端到端测试已覆盖错误 format/app hash、超量 chunk、非空目标、乱序传输、坏块重取、peer 拒绝、恢复后的 ICS23 proof、临时标记恢复和损坏标记拒绝。真实 CometBFT 新节点跨 P2P State Sync 与独立轻客户端证人仍属于后续 D-010 验收。

D-002 已实现版本 1 execution summary 与 compact block。两种产物共用 chain context、高度、链时间、最终 TCT 根和严格排序的系统公共事件；执行摘要逐笔绑定原始 envelope SHA-256、稳定结果码及成功交易的 tx/effect/action/fee，紧凑区块只保留成功交易的 nullifier、输出承诺、原始 OutputBody、memo 和动作。严格解码器执行字段数、长度、上限、顺序、成功语义和重新编码逐字节相等检查，域分离哈希分别使用 `bit/execution-summary/v1` 与 `bit/compact-block/v1`。

FinalizeBlock 已移除外部摘要注入接口。应用在同一候选事务中完成执行、预览最终 TCT 根、生成两种规范产物并写入 `execution/block/<height>` 和 `compact/hash/<height>`，ABCI `bit.block.v1` 事件公开同一摘要。Rust 与独立 Python oracle 共享空块黄金向量；全部系统事件形状另有往返测试。

四节点 CometBFT 探针现从两个冻结创世承诺执行一笔 2762 字节、2 Spend/2 Output 的真实 Groth16 Transfer。实测交易 ID 与 CometBFT hash 相同，四个应用的交易/nullifier ICS23 证明一致，两个输出推动共同 TCT 根；execution/compact 摘要同时匹配四份状态证明与 FinalizeBlock 事件。随后仍完成应用重启、H+2 奖励更新、真实重复投票处罚、Burn、验证人移除和失去超过三分之一投票权后的停机/恢复。

状态升级到 schema v17，并冻结 `supply/audit_snapshot` 版本 1。规范 CBOR 以 16 字节大端 Amount 编码完整供应恒等式、七类资产容器、完成 epoch 数和货币政策哈希；创世及每块提交和组成字段在同一个 JMT 批次落盘，重启要求严格解码结果与逐键重算完全一致。Rust/Python 共享黄金向量与字段篡改负例，状态测试验证创世和提交后 ICS23 成员证明；四节点探针进一步要求四份带证明快照字节相同。

区块 execution/compact 完整字节已接入本地不可变归档。Finalize 先把两份规范产物写入按高度隔离的 pending 目录并同步文件，JMT Commit 成功后才原子发布；正常读取不会看到未提交产物。启动恢复会清理高于 durable 高度的暂存，或在逐项重验 CBOR、共同块字段、成功交易集合、域分离哈希及链上摘要证明后，补完“状态已提交、归档未发布”的崩溃窗口。已发布高度不允许覆盖，读取时再次验证内容；状态同步不会把非共识归档混入 app hash。

状态升级到 schema v18。创世和每个区块都给九个子存储写入当前高度标记，使所有子树 JMT 版本、主树版本和 `meta/height` 严格一致；启动和精确历史查询都会校验这一约束。固定 Cnidarium 增加从 RocksDB 旧 JMT 节点重建统一版本只读快照的最小接口，因此进程缓存淘汰或应用重启后仍能生成指定已提交高度的成员/非成员 ICS23 证明。ABCI `height=0` 保持“最新”，正高度查询精确版本，未来或负高度稳定返回不支持。

四节点探针已改为在真实 Transfer 入块并继续推进至少两块后，仍按 Transfer 的精确高度查询交易、nullifier、TCT 根、供应审计和 execution/compact 摘要；四份结果必须一致且响应高度准确。应用 0 重启并追块后再次查询同一旧高度的 compact 摘要，验证历史快照确实来自耐久数据库而非进程缓存。

D-022 新增独立 `bit-gateway` workspace crate 和 OpenAPI 3.1 首批合同。Axum 路由直接共享 ABCI 的 `ApplicationCore`，提供 live/ready、带证明网络参数、白名单状态证明、完整供应审计 DTO 和有界 compact blocks；Amount/高度使用十进制字符串，规范字节与 protobuf proof 使用 Base64。`/v1/network` 与 `/v1/supply` 要求组成值来自同一 state height/app hash，并交叉核对规范货币政策及供应快照；供应响应已覆盖减半阶段、下一配额与发行完成状态。compact 每项必须通过本地归档严格解码、域分离哈希和精确历史状态证明三重核对。未接 TLS 时监听强制 loopback，批次最多 100 块、规范载荷最多 16 MiB，内部 frontier、版本标记和推进游标不对外反射。

## 验证结果

- `cargo fmt --all -- --check`：通过。
- `cargo clippy --workspace --all-targets --locked -- -D warnings`：BIT workspace 通过；固定 Penumbra 上游仍输出已记录 warning。
- `cargo test --workspace --locked`：105 个 Rust 测试通过，其中 `bit-types` 20 个、`bit-transaction` 7 个、`bit-shielded` 3 个、`bit-emission` 10 个、`bit-staking` 23 个、`bit-state` 21 个、`bit-app` 20 个、`bit-gateway` 1 个。
- Python oracle：6 个测试通过。
- 真实应用四节点 CometBFT：真实 Transfer、区块产物摘要/ABCI 事件/精确历史状态证明/本地归档一致、归档重启读取不变、应用重启后旧高度证明不变、四份供应审计快照证明一致、H+2、JMT 重启、真实重复投票广播与入块、验证人 H+2 移除、罚没 Burn、app hash 一致、处罚后投票权停机与恢复通过；证据见 `bit-app-network-result.json`。
- 统一证据：[development-baseline.json](D:/others/BIT/reports/development-baseline.json)。

## 当前限制与下一步

D-008 的退出资金闭环、证据校验/去重、永久 tombstone、P/X Burn、可恢复 SlashJob、Commit 前中断重放、同步 WriteBatch 与损坏批次预写拒绝、最后验证人耐久停签证据和四节点真实重复投票处罚已可运行。D-002 已有生产 execution/compact 规范编码、本地不可变归档、有界下载及多节点真实 Transfer 证据；D-004/D-005 已有绑定可信 app hash、逐块校验、隔离恢复和耐久切换的 ABCI State Sync。下一切片推进正式节点命令、网关头源/广播与多节点真实退出，随后继续真实磁盘耗尽/fsync 故障注入或 D-009/D-010 余下闭环。

四节点实验仍缺公网 TLS/Tor 网关、正式节点命令、多节点真实退出和独立 signer，不能视为主网节点验收。State Sync 仍缺真实 CometBFT 新节点联网演练、独立证人/可信期管理和跨平台恢复；D-004 还缺完整存储故障注入与长期历史版本增长验证。
