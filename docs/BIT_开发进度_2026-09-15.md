# BIT 开发进度（2026-09-15）

状态：D-002、D-004 至 D-009、D-022 和 D-025 继续推进，主网仍未就绪。按 D-001 至 D-030、完整产品、运维、外部审计和真实主网输入估算，完整上线约完成 33%–37%，链核心约完成 82%。本百分比是范围估算，不替代逐项验收；Android 按用户要求跳过。

## 本轮完成

ClaimGenesis 已从“只有 wire action 和授权角色”接通为完整链上动作。`bit-types` 固定 `claim_id = SHA256("bit/genesis-claim-id/v1" || chain_context || claim_pubkey || amount_be_16)`，拒绝零金额；`bit-transaction` 核对当前领取权、精确金额、未领取状态、GenesisClaim 域签名、费用来源、公开 release、Spend/Output 证明和 binding。

状态升级到 schema v20。`GenesisConfig` 接受公开领取清单，初始化拒绝非法 Ed25519 公钥、错误派生 ID、重复 ID/公钥、零金额和超过 G 容器的分配。按 claim ID 排序的清单摘要写入 `meta/genesis_claims_hash`，每项写入 `genesis/claims/<claim_id>`，成功领取记录共识高度，并与交易、nullifier、TCT、供应审计在同一个 JMT 批次提交。

重启校验会扫描全部领取记录，核对键、派生 ID、公钥、金额、领取高度和不可变配置，并要求“未映射的 G 余额 + 未领取条目总额”精确等于供应容器 G。ClaimGenesis 成功执行 `G -= amount; Q += amount-fee; F += fee`；同块和后续块不能再次释放同一领取权。

真实 Groth16 动作集成测试现覆盖 RegisterValidator、Delegate、Unbond、UpdateValidator、UnjailValidator、RotateConsensusKey、ClaimCommission 和 ClaimGenesis。新增 ClaimGenesis 用例生成真实 2 Spend/2 Output 证明，验证领取公钥签名、binding、费用、供应转换、领取高度、ICS23 成员证明和 RocksDB 重启恢复。轻量授权测试另覆盖错误金额和已领取状态。

只读网关 OpenAPI 升级到 0.3.1。`GET /v1/network` 返回 `genesis_manifest_hash` 与 `genesis_claims_hash` 及同高证明；白名单状态查询允许 `meta/genesis_manifest_hash` 和 `genesis/claims/<claim_id>` 的成员/非成员证明，内部清单或密钥材料不会暴露。OpenAPI YAML 和主网输入 JSON 已用工作区 Python 解析通过。

D-025 已形成首版设计，明确身份输入先产生 `genesis_manifest_hash` 和 chain context，再派生 claim/validator/position ID 与高度零状态，派生结果另行复核，从而消除循环哈希。`config/mainnet-inputs.template.json` 增加领取权和创世承诺输入槽位，并继续保持 `mainnet_ready=false`；没有填入虚构的主网参与者、公钥、金额或签名。

新增 `bit-genesis` crate 与 `bit` CLI。identity manifest v1 使用版本化 canonical CBOR，绑定源码 commit、crypto/参数文件哈希、asset/key/address 版本、资源限制、货币政策、唯一分配映射、领取权、私密承诺、初始验证者和审批阈值。清单哈希和审批签名分别域隔离，解码严格往返；共享向量固定完整 CBOR、manifest hash、chain context、签名消息和 2-of-2 测试签名包。

CLI 已实现 `genesis build|validate|verify|inspect|sign` 和 `release preflight`。分配表是 G0 的唯一计数源，领取权、承诺和自质押必须引用且只能引用同类 allocation，未映射、重复、零金额、错误总额、非法 Ed25519 公钥和未达签名阈值均拒绝。preflight 从证据计算结果，不使用 `mainnet_ready=true` 代替验证；空白主网模板会列出真实缺项并以非零状态退出。新增严格 JSON Schema，模板仍无主网假数据。

identity manifest 与运行时状态之间的首个信任锚已闭合：`GenesisConfig` 必须携带非零 `genesis_manifest_hash`，并强制 `chain_context = SHA256("bit/chain/v1" || genesis_manifest_hash)`。两者在高度零写入同一 JMT、重启逐字节核对，并由 `/v1/network` 返回同一状态版本下的 ICS23 证明。状态 schema 因新增不可变键提升到 v20。

D-025 第二阶段结果合同已冻结。`BIT-GENESIS-DERIVED` v1 绑定 identity hash、chain context、运行时输入文件哈希、全部派生 ID/共识地址/投票权、领取与承诺摘要、TCT 根、app hash、创世区块产物哈希及 CometBFT genesis 文件哈希。独立的 `BIT-GENESIS-DERIVED-APPROVAL-V1` 签名域沿用 identity 中的签署者集合和阈值；CLI 新增 `build-derived|verify-derived|inspect-derived|sign-derived`，preflight 会核对第二阶段阈值与所有实际文件哈希。共享向量同时由 Rust 和 Python oracle 复算。

D-025 确定性创世物化器已经落地。`BIT-GENESIS-RUNTIME-INPUTS` v1 以严格 JSON 合同绑定费用、质押、处罚、CometBFT 参数、验证人元数据和真实 512 字节恢复收据，其原始文件 SHA-256 必须等于 identity 中已签署的参数哈希；金额与总投票权上限用规范十进制字符串，避免跨语言整数舍入。未知字段、乱序或缺失验证人、全零收据、无效参数、签名未达阈值和 hash 不一致都会在写入前失败。

`bit genesis materialize` 现在从已批准 identity 和运行时输入真实构造 `GenesisConfig`、创世领取权、私密承诺、费用储备、已激活自质押、验证人集合及高度零 Cnidarium/JMT/TCT 状态，生成 app hash、CometBFT v0.38 `genesis.json`、derived manifest 和构建报告。构建在同级临时目录完成并以单次 rename 发布，拒绝覆盖已有目标。两次独立构建的协议产物逐字节一致；黄金向量由 Rust 测试锁定，由 Go module v0.38.23 构建的实际 CometBFT 二进制（自报 0.38.22）已成功解析生成的 genesis 并进入 ABCI 连接阶段。

正式节点入口与独立 bundle 重放已经落地。`bit genesis verify-bundle` 验证 identity 和 derived 两阶段阈值，在全新临时状态中重新物化并逐字节比较 identity、签名、运行时输入、CometBFT genesis、derived 和报告；bundle 内 RocksDB 不作为可信节点状态。输入限制为普通文件/目录，并拒绝符号链接、超限文件、非规范 JSON、hash 或重放差异。

`bit-node start` 只从验证后的 bundle 重构 `GenesisConfig` 与完整 `RequestInitChain`，拒绝本地共识参数覆盖、非 loopback ABCI 和 bundle/state 路径重叠。真实单节点 CometBFT 已完成两阶段签名、清空状态重放、严格 InitChain、连续出块、区块 1 创世 app hash 核对、初始投票权核对、ICS23 manifest 证明和成对重启续块。联调还发现并修复了 CometBFT 会原样传递 `app_state` JSON 字节的问题，节点现在保留已重放 `genesis.json` 的原始片段。

## 验证结果

- `cargo fmt --all -- --check`：通过。
- `cargo clippy --workspace --all-targets --locked -- -D warnings`：BIT workspace 通过；固定 Penumbra 上游 warning 与既有基线一致。
- `cargo test --workspace --locked`：126 个 BIT Rust 测试通过，其中 `bit-types` 20 个、`bit-genesis` 16 个、`bit-transaction` 8 个、`bit-shielded` 3 个、`bit-emission` 10 个、`bit-staking` 23 个、`bit-state` 22 个、`bit-app` 23 个、`bit-gateway` 1 个。
- 固定 Cnidarium：27 个测试通过。
- Python oracle：8 个测试通过；独立核对 genesis identity/derived canonical CBOR、chain context 和两阶段审批签名消息域。
- 正式创世节点：真实 CometBFT InitChain、区块 1 app hash、验证人 power、ICS23 manifest 证明及应用/共识成对重启通过。
- 真实四节点 CometBFT：Transfer、区块产物、精确历史证明、H+2、JMT 重启、重复投票处罚、Burn、验证人移除和法定票数停机/恢复继续通过。
- 统一报告：`reports/development-baseline.json`，完成时间 `2026-09-15T06:14:51+08:00`，状态 `PASS`。

## 当前限制与下一步

创世 identity/derived manifest、两阶段签名包、运行时输入合同、离线物化器、独立重放器和正式节点入口已有机器合同；仍缺检查点、审查和发布材料的强类型证据合同、初始私密承诺的资产语义验证和生产服务管理。真实主网分配、公钥、初始验证人、恢复收据、检查点发布者和审查材料仍只能由真实参与者提供。

下一切片优先把 ClaimGenesis 放入真实四节点广播和崩溃重放实验，并推进正式节点的 State Sync 服务配置。后续仍包括网关已验证头源与广播、真实 CometBFT 新节点 State Sync、钱包核心和完整客户端。
